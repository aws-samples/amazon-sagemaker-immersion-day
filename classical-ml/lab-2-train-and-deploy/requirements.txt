# =============================================================================
# SageMaker Immersion Day - Lab 2 (Train and Deploy) - self-contained kernel deps
# =============================================================================
# Complete notebook-KERNEL dependency set for this lab. Install this file alone
# to provision the kernel - Setup.ipynb is NOT required:
#
#     import sys
#     !uv pip install --python {sys.executable} -r requirements.txt
#
# This inlines the shared SageMaker Distribution kernel stack. Lab 2 needs no
# lab-specific extras beyond the shared stack. Any custom-container / training
# build dependencies live with their own Dockerfile/source and are NOT here.
# Tested against SageMaker Distribution 4.x (Python 3.12), SageMaker SDK 3.13.1.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# --- SageMaker Python SDK v3 suite ---
sagemaker==3.13.1
sagemaker-core==2.13.1
sagemaker-train==1.13.1
sagemaker-serve==1.13.1
sagemaker-mlops==1.13.1

# --- Experiment tracking (MLflow) ---
mlflow<4
sagemaker-mlflow==0.3.0

# --- Data wrangling / visualization ---
pandas>=2,<3
matplotlib<4
awswrangler==3.5.1

# --- Classical ML utilities ---
scikit-learn==1.4.2
xgboost==3.0.5
shap
evidently>=0.7.20

# --- Deep learning (CPU wheels via the extra-index-url above) ---
torchvision
