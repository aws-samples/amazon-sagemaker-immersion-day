# =============================================================================
# SageMaker Immersion Day - Lab 4 (Model Monitoring) - self-contained kernel deps
# =============================================================================
# Complete notebook-KERNEL dependency set for this lab. Install this file alone
# to provision the kernel - Setup.ipynb is NOT required:
#
#     import sys
#     !uv pip install --python {sys.executable} -r requirements.txt
#
# This inlines the shared SageMaker Distribution kernel stack. Lab 4 needs no
# lab-specific kernel extras. The monitoring/training scripts this lab writes to
# scripts/requirements.txt run INSIDE remote jobs and are NOT folded in here.
# Tested against SageMaker Distribution 4.x (Python 3.12), SageMaker SDK 3.13.1.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# --- SageMaker Python SDK v3 suite ---
sagemaker==3.13.1
sagemaker-core==2.13.1
sagemaker-train==1.13.1
sagemaker-serve==1.13.1
sagemaker-mlops==1.13.1

# --- Experiment tracking (MLflow) ---
mlflow<4
sagemaker-mlflow>=0.5.0,<1

# --- Data wrangling / visualization ---
pandas>=2,<3
matplotlib<4
awswrangler==3.5.1

# --- Classical ML utilities ---
scikit-learn==1.4.2
xgboost==3.0.5
shap
evidently>=0.7.20

# --- Deep learning (CPU wheels via the extra-index-url above) ---
torchvision
